touch .gitignore  === for creating folder to ignore any module
git config --global user.name "your name"
git config --global user.email "youremail.example.com"
git init      === for initialing  git repository
git add .   ==== all files going to staging area except ignoring file\
git commit -m "initial commit"   === our commit will generate/create
git status ===== just to se working tree
git ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
for this command your ssh key will generate. then add this key to
your github account.
git remote add origin <ssh url.git
git push origin master === now your project uploaded to github.